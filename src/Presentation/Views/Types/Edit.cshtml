@{
    ViewData["Title"] = "Types";
}

@model Presentation.Models.TypeModel

<div class="row">
    <div class="col-md-12">
        <h1>Edit Type</h1>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <form enctype="multipart/form-data" asp-action="OnPut" asp-anti-forgery="true" class="w-100 bg-white p-3 mt-3 border" method="post">
            <div class="form-row">
                <div class="col-md-12">
                    <div class="mb-2 ml-3" asp-validation-summary="All"></div>
                </div>
                <input id="type-id" type="hidden" asp-for="Id"/>
            </div>
            <div class="form-row">
                <div class="col-md-12">
                    <div class="mb-2 ml-3" asp-validation-summary="All"></div>
                </div>
            </div>
            <div class="form-row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="Name" for="type-name">Name</label>
                        <input asp-for="Name" type="text" class="form-control" id="type-name"
                        aria-describedby="type-name" placeholder="Enter a type name">
                        <small id="product-type-help" class="form-text text-muted">
                        e.g: Xiaomi
                        </small>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <div class="form-group">
                            <label asp-for="Category" for="product-category">Category</label>
                            <select asp-for="Category" id="product-category" class="form-control">
                                <option selected>Choose...</option>
                            </select>
                            <small id="product-category" class="form-text text-muted">
                            Don't find what you want? <a asp-controller="Categories" asp-action="Create">create a new category</a>
                            </small>
                        </div>
                    </div>
                </div>
            </div>
            <div class="form-row">
                <div class="col-md-12">
                    <div class="form-group">
                        <label asp-for="Description" for="type-description">Description</label>
                        <textarea asp-for="Description" class="form-control" id="type-description"
                        rows="3" maxlength="500"></textarea>
                        <small id="type-description-help" class="form-text text-muted">
                        max: 500 characters
                        </small>
                    </div>
                </div>
            </div>
            <div class="form-row d-flex justify-content-end">
                <button type="button" class="btn btn-secondary mr-3" data-toggle="modal" data-target="#delete-modal">
                Delete
                </button>
                <div class="modal fade" id="delete-modal" tabindex="-1" role="dialog" aria-labelledby="delete-modal" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="delete-modal-label">Delete Record</h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            <p>Are you sure want to delete this record?</p>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            <button type="button" class="btn btn-danger" id="btn-delete">Yes, delete</button>
                        </div>
                        </div>
                    </div>
                </div>
                <button class="btn btn-primary" type="submit">Save</button>
            </div>
        </form>
    </div>
</div>
@section scripts{
    <script src="~/js/types.js"></script>
}