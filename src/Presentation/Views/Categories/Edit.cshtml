@{
    ViewData["Title"] = "Categories";
}

@model Presentation.Models.CategoryModel

<div class="row">
    <div class="col-md-12">
        <h1>Edit Category</h1>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <form enctype="multipart/form-data" asp-action="OnPut" asp-anti-forgery="true" class="w-100 bg-white p-3 mt-3 border" method="post">
            <div class="form-row">
                <div class="col-md-12">
                    <div class="mb-2 ml-3" asp-validation-summary="All"></div>
                </div>
                <input id="category-id" type="hidden" asp-for="Id"/>
            </div>
            <div class="form-row">
                <div class="col-md-12">
                    <div class="form-group">
                        <label asp-for="Name" for="category-name">Name</label>
                        <input asp-for="Name" type="text" class="form-control" id="category-name"
                        aria-describedby="product-name" placeholder="Enter a category name">
                        <small id="category-name-help" class="form-text text-muted">
                        e.g: Smarthphone
                        </small>
                    </div>
                </div>
            </div>
            <div class="form-row">
                <div class="col-md-12">
                    <div class="form-group">
                        <label asp-for="Description" for="category-description">Description</label>
                        <textarea asp-for="Description" class="form-control" id="category-description"
                        rows="3" maxlength="500"></textarea>
                        <small id="category-description-help" class="form-text text-muted">
                        max: 500 characters
                        </small>
                    </div>
                </div>
            </div>
            <div class="form-row d-flex justify-content-end">
                <button type="button" class="btn btn-secondary mr-3" data-toggle="modal" data-target="#delete-modal">
                Delete
                </button>
                <div class="modal fade" id="delete-modal" tabindex="-1" role="dialog" aria-labelledby="delete-modal" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="delete-modal-label">Delete Record</h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            <p>Are you sure want to delete this record?</p>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            <button type="button" class="btn btn-danger" id="btn-delete">Yes, delete</button>
                        </div>
                        </div>
                    </div>
                </div>
                <button class="btn btn-primary" type="submit">Save</button>
            </div>
        </form>
    </div>
</div>

@section scripts{
    <script src="~/js/categories.js"></script>
}